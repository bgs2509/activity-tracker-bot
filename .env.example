# =============================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env and fill in your actual values
#
# SECURITY WARNING: Never commit .env file to git!
# The .env file contains sensitive secrets and should be kept private.
#
# For running tests, use .env.test (already contains safe dummy values)
# =============================================================================

# =============================================================================
# Telegram Bot Configuration
# =============================================================================
# Get your bot token from @BotFather on Telegram
# Format: {bot_id}:{auth_token}
TELEGRAM_BOT_TOKEN=your_bot_token_here

# =============================================================================
# PostgreSQL Database Configuration
# =============================================================================
# Database credentials for PostgreSQL container
POSTGRES_USER=tracker_user
POSTGRES_PASSWORD=tracker_password
POSTGRES_DB=tracker_db

# =============================================================================
# Data API Configuration
# =============================================================================
# Database connection URL for data_postgres_api service
# Format: postgresql+asyncpg://user:password@host:port/database
# Note: Use 'postgres' as hostname when running in Docker Compose
DATABASE_URL=postgresql+asyncpg://tracker_user:tracker_password@postgres:5432/tracker_db

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection for FSM (Finite State Machine) storage
# Note: Use 'redis' as hostname when running in Docker Compose
REDIS_URL=redis://redis:6379/0

# =============================================================================
# Internal Services Configuration
# =============================================================================
# URL to data_postgres_api service (used by tracker_activity_bot)
# Note: Use service name 'data_postgres_api' when running in Docker Compose
DATA_API_URL=http://data_postgres_api:8000

# =============================================================================
# Application Settings
# =============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# AI Integration Configuration (OpenRouter)
# =============================================================================
# OpenRouter API key for AI-powered activity parsing
# Get your API key from https://openrouter.ai/keys
# This enables natural language activity input and intelligent parsing
OPENROUTER_API_KEY=your_openrouter_api_key_here
