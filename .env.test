# =============================================================================
# TEST ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains SAFE dummy values for running tests.
# NO real secrets - safe to commit to git.
#
# These values are used by pytest when running unit tests to satisfy
# required environment variable validation in pydantic Settings classes.
#
# IMPORTANT: All external dependencies (database, Redis, Telegram API)
# are mocked in unit tests, so these values are NEVER used for actual
# connections. They only need to pass format validation.
#
# For production/development with real services, create a .env file
# based on .env.example (see README.md for instructions).
# =============================================================================

# =============================================================================
# Telegram Bot Configuration
# =============================================================================
# Dummy bot token with valid format (10 digits:43 chars)
# Format: {bot_id}:{auth_token}
# This token is FAKE and will never connect to real Telegram API
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-DUMMY-TEST-TOKEN

# =============================================================================
# PostgreSQL Database Configuration
# =============================================================================
# Test database credentials (used in format validation only)
# Unit tests mock all database interactions
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
POSTGRES_DB=test_db

# Database connection URL for data_postgres_api service
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://test_user:test_password@localhost:5432/test_db

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection for FSM storage (mocked in unit tests)
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# Internal Services Configuration
# =============================================================================
# URL to data_postgres_api service (used by tracker_activity_bot)
# In unit tests, HTTP client is mocked, so this URL is never called
DATA_API_URL=http://localhost:8000

# =============================================================================
# Application Settings
# =============================================================================
# Logging level for test output (DEBUG provides detailed test information)
LOG_LEVEL=DEBUG

# Auto-create database tables (disabled for tests - migrations should be used)
ENABLE_DB_AUTO_CREATE=false
